# LSTM configuration
data:
  dataset_path: "data/raw/CEAS_08.csv"
  train_split: 0.8
  batch_size: 32
  max_tokens: 2000

model:
  type: "lstm"
  dropout_rate: 0.2
  num_layers: 1

train:
  epochs: 5
  learning_rate: 0.001
  optimizer: "adam"
  seed: 42

output_dir: "results/lstm_single_dr02/"
